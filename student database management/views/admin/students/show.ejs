<html>
<head>
    <title>Show Students</title>
    <link rel="stylesheet" type="text/css" href="../css/styles.css">
</head>
<body>
<div class="path">
    <a href="/admin">admin panel</a>
</div>
<h1>Show Students</h1>
<% if (warningMessage) { %>
    <p class='warning-message'>
        <% var warnings = warningMessage.split("\n");
        for (var i = 0; i < warnings.length; i++) { if (!warnings[i].length) continue %>
        <%= warnings[i] %> <br>
        <% } %>
    </p>
<% } %>
<form action="/admin/show-students" method="post" class="search-bar">
    <label>Search by:</label>
    <select id="searchOption" name="searchOption">
        <option value="studentID" selected>Student ID</option>
        <option value="level">Level</option>
        <option value="gpa">GPA</option>
    </select>

    <div id="studentIDInputs" style="display:block">
        <input type="text" name="studentIDFrom" placeholder="Enter student ID range start">
        <input type="text" name="studentIDTo" placeholder="Enter student ID range end">
    </div>

    <div id="gpaInputs" style="display:none">
        <input type="text" name="gpaFrom" placeholder="Enter GPA range start">
        <input type="text" name="gpaTo" placeholder="Enter GPA range end">
    </div>

    <div id="levelInputs" style="display:none">
        <input type="text" name="level" placeholder="Enter level">
    </div>
    <div class="searchButton">
        <button type="submit">Search</button>
    </div>
</form>
<hr class="separator">
<div id="student-card-area">
    <% if (students) { %>
        <% for (i = 0; i < students.length; i++) { %>
            <div class="student-card">
                <h2><%= students[i].firstName %> <%= students[i].lastName %></h2>
                <p>Student ID: <%= students[i].id %></p>
                <p>Level: <%= students[i].level %></p>
                <p>GPA: <%= students[i].gpa %></p>
            </div>
        <% } %>
    <% } %>
</div>
<script src="../../javascript/admin/show-students.js"></script>
</body>
</html>





